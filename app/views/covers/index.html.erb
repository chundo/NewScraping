<p id="notice"><%= notice %></p>

<h1>Covers</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Sumary</th>
      <th>Image</th>
      <th>Action url</th>
      <th>Status</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @covers.each do |cover| %>
      <tr>
        <td><%= cover.name %></td>
        <td><%= cover.sumary %></td>
        <td><%= cover.image %></td>
        <td><%= cover.action_url %></td>
        <td><%= cover.status %></td>
        <td><%= link_to 'Show', cover %></td>
        <td><%= link_to 'Edit', edit_cover_path(cover) %></td>
        <td><%= link_to 'Destroy', cover, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Cover', new_cover_path %>
